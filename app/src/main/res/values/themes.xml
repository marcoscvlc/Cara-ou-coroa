<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CaraOuCoroa" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:colorBackground">@color/verde_claro</item>
        <item name="android:navigationBarColor">@color/verde_claro</item>
        <item name="android:statusBarColor">@color/Verde_medio</item>
    </style>

    <style name="Theme.CaraOuCoroa" parent="Base.Theme.CaraOuCoroa" />
</resources>